!    -*- f90 -*-
! Note: the context of this file is case sensitive.

python module matmult ! in 
    interface  ! in :matmult
        subroutine matmult(m1,m2,d1,prod) ! in :matmult:matmult.f90
            complex dimension(d1,d1),intent(in) :: m1
            complex dimension(d1,d1),intent(in),depend(d1,d1) :: m2
            integer, optional,check(shape(m1,0)==d1),depend(m1) :: d1=shape(m1,0)
            complex dimension(d1,d1),intent(out),depend(d1,d1) :: prod
        end subroutine matmult
        subroutine doublesum(ket,bra,m,states,tessere,prod) ! in :math_functions:math_functions.f90
            complex dimension(states), intent(in) :: ket
            complex dimension(states),depend(states),intent(in) :: bra
            complex dimension(tessere,states,states),depend(states,states),intent(in) :: m
            integer, optional,check(len(ket)>=states),depend(ket) :: states=len(ket)
            integer, optional,check(shape(m,0)==tessere),depend(m) :: tessere=shape(m,0)
            complex dimension(tessere),depend(tessere),intent(out) :: prod
        end subroutine doublesum
        subroutine single_summation_tessere(ket,m,states,tessere,prod) ! in :math_functions:math_functions.f90
            complex dimension(tessere),intent(in) :: ket
            complex dimension(tessere,states,states),depend(tessere),intent(in) :: m
            integer, optional,check(shape(m,1)==states),depend(m) :: states=shape(m,1)
            integer, optional,check(len(ket)>=tessere),depend(ket) :: tessere=len(ket)
            complex dimension(states,states),depend(states,states),intent(out) :: prod
        end subroutine single_summation_tessere
    end interface 
end python module matmult

! This file was auto-generated with f2py (version:2).
! See http://cens.ioc.ee/projects/f2py2e/
